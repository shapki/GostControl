<Window x:Class="GostControl.AppWindows.AddEditRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Редактирование номера" 
        Height="550" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="NoResize"
        MinHeight="550" MinWidth="600">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                                <Setter Property="Foreground" Value="#757575"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок (не прокручивается) -->
        <Border Grid.Row="0" Background="#2196F3" CornerRadius="8" Padding="15" Margin="20,20,20,10">
            <TextBlock x:Name="TitleTextBlock" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Text="Редактирование номера"/>
        </Border>

        <!-- Форма редактирования (прокручивается) -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20,0,20,20">
            <Border Background="White" CornerRadius="8" Padding="20"
                    BorderBrush="#E0E0E0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Номер комнаты -->
                    <TextBlock Grid.Row="0" Grid.Column="0" 
                               Text="Номер комнаты:" 
                               Margin="0,0,10,10" 
                               VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" 
                             x:Name="RoomNumberTextBox"
                             Text="{Binding Room.RoomNumber, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,10" 
                             Height="30" 
                             Padding="5"/>

                    <!-- Категория -->
                    <TextBlock Grid.Row="1" Grid.Column="0" 
                               Text="Категория:" 
                               Margin="0,0,10,10" 
                               VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" 
                              x:Name="CategoryComboBox"
                              DisplayMemberPath="CategoryName"
                              SelectedValuePath="CategoryID"
                              SelectedValue="{Binding Room.CategoryID}"
                              ItemsSource="{Binding Categories}"
                              Margin="0,0,0,10" 
                              Height="30" 
                              Padding="5"/>

                    <!-- Этаж -->
                    <TextBlock Grid.Row="2" Grid.Column="0" 
                               Text="Этаж:" 
                               Margin="0,0,10,10" 
                               VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" 
                              x:Name="FloorComboBox"
                              SelectedItem="{Binding Room.Floor}"
                              Margin="0,0,0,10" 
                              Height="30" 
                              Padding="5">
                        <ComboBoxItem>1</ComboBoxItem>
                        <ComboBoxItem>2</ComboBoxItem>
                        <ComboBoxItem>3</ComboBoxItem>
                        <ComboBoxItem>4</ComboBoxItem>
                        <ComboBoxItem>5</ComboBoxItem>
                    </ComboBox>

                    <!-- Балкон -->
                    <CheckBox Grid.Row="3" Grid.Column="1" 
                              x:Name="HasBalconyCheckBox"
                              Content="Наличие балкона"
                              IsChecked="{Binding Room.HasBalcony}"
                              Margin="0,0,0,10"
                              VerticalAlignment="Center"/>

                    <!-- Доступность -->
                    <CheckBox Grid.Row="4" Grid.Column="1" 
                              x:Name="IsAvailableCheckBox"
                              Content="Номер доступен"
                              IsChecked="{Binding Room.IsAvailable}"
                              Margin="0,0,0,10"
                              VerticalAlignment="Center"/>

                    <!-- Информация о категории -->
                    <Border Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                            Background="#F5F5F5" 
                            CornerRadius="5" 
                            Padding="10" 
                            Margin="0,10,0,0">
                        <StackPanel>
                            <TextBlock Text="Информация о выбранной категории:" 
                                       FontWeight="SemiBold" 
                                       Margin="0,0,0,5"/>
                            <TextBlock x:Name="CategoryInfoText" 
                                       TextWrapping="Wrap"
                                       MinHeight="60"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </ScrollViewer>

        <!-- Кнопки (не прокручиваются) -->
        <Border Grid.Row="2" Background="White" Padding="20" Margin="20,0,20,20"
                BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" 
                        Style="{StaticResource ModernButton}"
                        Background="#F44336"
                        Width="100"
                        Margin="0,0,10,0"
                        Click="CancelButton_Click"/>
                <Button Content="Сохранить" 
                        Style="{StaticResource ModernButton}"
                        Background="#4CAF50"
                        Width="100"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>