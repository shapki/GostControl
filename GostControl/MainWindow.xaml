<Window x:Class="GostControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GostControl.AppViewModels"
        mc:Ignorable="d"
        Title="ГостКонтроль - Управление гостиницей" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:NullToBoolConverter x:Key="NullToBoolConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <ToolBar Grid.Row="0">
            <Button Content="Обновить" Command="{Binding LoadClientsCommand}" Margin="5"/>
            <Button Content="Добавить клиента" Command="{Binding AddClientCommand}" Margin="5"/>
            <Button Content="Удалить клиента" Command="{Binding DeleteClientCommand}" 
                    Margin="5" IsEnabled="{Binding SelectedClient, Converter={StaticResource NullToBoolConverter}}"/>
            <Separator/>
            <TextBlock Text="Всего клиентов:" Margin="5"/>
            <TextBlock Text="{Binding Clients.Count}" Margin="5" FontWeight="Bold"/>
        </ToolBar>

        <!-- Список клиентов -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Clients}"
                  SelectedItem="{Binding SelectedClient}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ClientID}" Width="50"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="120"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding MiddleName}" Width="120"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding PhoneNumber}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Дата регистрации" 
                                    Binding="{Binding RegistrationDate, StringFormat=dd.MM.yyyy}" 
                                    Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Статус бар -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="Готово"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>